start_mowing:
  target:
    device:
      integration: ecovacs_goat_openapi
  fields:
    config_entry_id:
      selector:
        text:
      required: false
      example: "01H..."
    nickname:
      selector:
        text:
      required: false
      example: "Mähmet"

refresh:
  target:
    device:
      integration: ecovacs_goat_openapi
  fields:
    config_entry_id:
      selector:
        text:
      required: false
      example: "01H..."
